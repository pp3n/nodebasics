//Boolean values can be used for data validation. 
//There are only 2 states for Boolean values (true or false)

var isValid = true;

if (!isValid) {
    console.log('Data is Valid');
} else {
    console.log('Data is invalid');
}