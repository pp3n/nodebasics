var pet = {
    name: 'Johny',
    type: 'German Sheperd'
}

function printPetDetails(pet) {
    console.log("You own a dog named" + ' ' + pet.name + ' ' + 'and the breed is' + ' ' + pet.type);
}

printPetDetails(pet);

